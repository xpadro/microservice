spring.application.name=bookshelf-service
spring.profiles.active=dev

# Spring Cloud Config Server
spring.cloud.config.uri=http://localhost:8888

# Eureka Server
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
logging.level.com.netflix=DEBUG


# Access to localhost
# If this app has to access another IP on the host machine when executed inside a Docker container,
# we have to attach an IP alias to your host network interface instead of 'localhost' by executing the
# following command:

# > sudo ifconfig lo0 alias 123.123.123.123/24

# See: https://stackoverflow.com/a/31328031/1900288